# Handover pack (advisory)

On-call receives:

1. `GET /ops/slo` and last `decision_id`s (not CoT).
2. Open identity conflicts and PLT recovery blockers (CURRENT ≠ ready).
3. CASCADE-001 analogue: SOC isolate is an **input**, PE warning is dissent — ABSTAIN unless CTQ-ISO, still no execute.
4. Untrusted: `data/shadow/shift_handover_email.txt`, shadow spreadsheet.
5. Kill switch: `AI_ENABLED=0`.
6. RACI: roles only (OPEN-001).

Do not hand over an isolate-execute runbook. There is none.
