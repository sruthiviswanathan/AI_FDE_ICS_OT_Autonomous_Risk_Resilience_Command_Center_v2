# Dev: Vite proxies /api/* → backend (see vite.config.ts)
VITE_API_URL=/api

# If port 8000 is blocked or runs a stale service, override in .env.development.local:
VITE_DEV_API_TARGET=http://127.0.0.1:8000
