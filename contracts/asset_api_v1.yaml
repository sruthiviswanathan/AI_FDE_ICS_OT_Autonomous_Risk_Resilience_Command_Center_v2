openapi: 3.0.0
info: {title: Legacy Asset API v1, version: '1'}
paths:
  /assets/{id}:
    get:
      responses:
        '200': {description: OK}
# v1 field convention: assetId, fwVersion, operationalState
